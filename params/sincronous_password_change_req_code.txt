// Start a progress dialog
			ProgressDialogFragment fragment = ProgressDialogFragment.newInstance();
			fragment.show(this.getSupportFragmentManager(), DIALOG_PACKAGE_LOAD);
			// Register a broadcast receiver
			this.brPasswordChange = new PasswordChangeBroadcastReceiver();
			IntentFilter filter = new IntentFilter(Mbi.EVENT_PASSWORD_CHANGED);
			LocalBroadcastManager.getInstance(this).registerReceiver(this.brPasswordChange, filter);
// Retrieve username
			String username = this.getIntent().getStringExtra(
					Mbi.EXTRA_USERNAME);

			// Request password change
			AuthenticationCore.getInstance().changeAccountablePassword(
					username, currentPassword, newPassword);